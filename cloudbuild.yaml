steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: [ 'build', '-t', 'gcr.io/"joes-project-379413"/hello-cloudbuild', '.']
  # push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/"joes-project-379413"/hello-cloudbuild']
images:
  - 'gcr.io/"joes-project-379413"/hello-cloudbuild'
- logging: 'NONE'


